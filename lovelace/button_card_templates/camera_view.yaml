camera-view:
  template: camera-status
  tap_action:
    action: fire-dom-event
    browser_mod:
      command: popup
      hide_header: false
      title: Live View
      card:
        type: 'custom:webrtc-camera'
        entity: |
          [[[ return variables.stream; ]]]